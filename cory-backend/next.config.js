/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost','kuriyamacory.com'],
  },
  basePath: '/himitukichi',
}

module.exports = nextConfig
